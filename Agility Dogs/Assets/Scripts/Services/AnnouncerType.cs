namespace AgilityDogs.Services
{
    /// <summary>
    /// Which announcer voice a commentary line belongs to.
    /// Main = Arthur (play-by-play), Color = Buck (color commentary), PA = venue announcer.
    /// </summary>
    public enum AnnouncerType
    {
        Main,
        Color,
        PA
    }
}
